---
title: "Swap bitcoin to USDT"
description: "A BTC to USDT swap end to end, from balance check through to the confirmed stablecoin balance."
---
This tutorial shows you how to convert Bitcoin (BTC) from a user’s Bitnob wallet into USDT using Bitnob’s built-in swap engine. You’ll go from checking balances to completing a swap and confirming the new stablecoin balance.

This is useful when:

- Users want to lock in the value of their BTC
- You want to stabilize treasury or float in stablecoins
- You’re enabling BTC payments but settling in USDT

## Key concepts

Swaps allow users to convert volatile assets (BTC) into stable assets (USDT) inside Bitnob. Bitnob handles the exchange rate matching, balance updates, and fees all through simple API calls.

By default, every Bitnob account has a BTC and USDT wallet created automatically. You don’t need to create these.

## Prerequisites

A Bitnob sandbox or production API key

The user must have BTC in their wallet (from on-chain or Lightning)

You should track reference IDs for each swap

## Step 1: Check BTC wallet balance

To confirm the user has sufficient funds to swap, check their BTC balance with [Get Balances](/api-reference/balances/list-balances), or [Get Balance By Currency](/api-reference/balances/get-balance-by-currency) for a single asset.

```bash
GET /api/balances/BTC
```

If you're listing balances across every currency:

```bash
GET /api/balances
```

From the response, look for the wallet with "currency": "BTC" and check the availableBalance.

## Step 2: Get a swap quote (optional)

You can fetch a live rate before executing the swap with [Quotes](/api-reference/trading/create-quote). This allows you to show the user what they’ll receive.

```bash
POST /api/trading/quotes
```

**Example request:**

```json
{
    "base_currency": "BTC",
    "quote_currency": "USDT",
    "quantity": "10",
    "side": "sell"
}
```

**Example response:**

```json
{
    "success": true,
    "message": "Quote retrieved successfully",
    "data": {
        "quote": {
            "id": "453a50b1-4fb6-49d9-ba80-3c59af049fd5",
            "base_currency": "BTC",
            "quote_currency": "USDT",
            "side": "SELL",
            "quantity": "10",
            "price": "68807.23120975",
            "expires_at": "2026-03-22T13:10:46.771686117Z",
            "created_at": "2026-03-22T13:10:16.771686367Z",
            "original_quantity": "10",
            "consumed_quantity": "0",
            "remaining_quantity": "10",
            "exchange": {
                "send_quantity": "10.00000000",
                "send_currency": "BTC",
                "receive_quantity": "688072.31",
                "receive_currency": "USDT"
            }
        }
    },
    "metadata": {
        "request_id": "trading_req_019d15aa-ecb4-7d55-90a1-16784f39c5a5"
    },
    "timestamp": "2026-03-22T13:10:16.790192295Z"
}
```

The quote expires in 60 seconds. You should show this info to the user (rate, fees, amount they’ll receive) before confirming the swap.

## Step 3: Execute the quote

To perform the conversion, send a swap request: see [Create Order](/api-reference/trading/submit-order), and [Get Order By ID](/api-reference/trading/get-order-by-id-or-reference) to confirm it settled.

```bash
POST /api/trading/orders
```

**Example request:**

```bash
curl --location 'https://api.bitnob.com/api/trading/orders' \
--header 'Content-Type: application/json' \
--header 'x-auth-client: CLIENT_ID' \
--header 'x-auth-timestamp: TIMESTAMP' \
--header 'x-auth-nonce: NONCE' \
--header 'x-auth-signature: SIGNATURE' \
--data '{
    "base_currency": "BTC",
    "quote_currency": "USDT",
    "side": "sell",
    "quantity": "0.0001",
    "price": "88989.119625",
    "quote_id": "b8da95f6-13b6-41f3-9675-9d24535df1b3",
    "metadata": {
        "reference": "user-btc-sell"
    }
}'
```

**Example response:**

```json
{
    "success": true,
    "message": "Order submitted successfully",
    "data": {
        "order": {
            "id": "ae56c2eb-74a4-40c6-bb5f-acda2d0e55bb",
            "base_currency": "USDT",
            "quote_currency": "BTC",
            "side": "SELL",
            "order_type": "limit",
            "quantity": "10",
            "price": "68860.05496125",
            "status": "filled",
            "created_at": "2026-03-22T13:15:20.595989775Z",
            "updated_at": "2026-03-22T13:15:20.595989775Z",
            "filled_quantity": "10",
            "remaining_quantity": "0",
            "quote_id": "e30a92b5-bdce-4df2-849d-f4295f4ffb72"
        },
        "fills": [
            {
                "id": "19ffd357-f494-400a-b609-0f7904934ec2",
                "order_id": "ae56c2eb-74a4-40c6-bb5f-acda2d0e55bb",
                "quantity": "9.99",
                "created_at": "2026-03-22T13:15:21.621546159Z"
            }
        ],
        "quote_consumption": {
            "quote_id": "e30a92b5-bdce-4df2-849d-f4295f4ffb72",
            "consumed_quantity": "10",
            "remaining_quantity": "0",
            "is_exhausted": true
        }
    },
    "metadata": {
        "request_id": "trading_req_019d15af-8f82-766e-85ad-50216f8de0ce"
    },
    "timestamp": "2026-03-22T13:15:21.648920462Z"
}
```

This response confirms that the BTC was successfully converted to stablecoin and credited to the appropriate wallet.

## Step 4: Confirm stablecoin wallet balance

To verify the updated balance, call:

```bash
GET api/wallets/usdt
```

Based on the currency you swapped into.

The wallet response will include fields like availableBalance,totalReceived , and currency.

## Handling errors

If the user has insufficient BTC, you’ll get a 400 error with a descriptive message

If the quote has expired, you’ll be asked to fetch a new one

If the wallet is locked or unavailable, the swap will fail

Always log the swapId and reference for reconciliation

## Best practices

Always fetch a quote before swapping, especially if you're building a user-facing UI

Show a timer for the quote expiry if you use quotes in the front end

Clearly display fees and rates for transparency

Use unique reference values per swap so you can audit them later Avoid executing back-to-back swaps without confirming balances

## Summary

With just a few API calls, you can enable users to:

- Convert Bitcoin (on-chain or Lightning) into stablecoins
- Lock in value without withdrawing funds
- Prepare for local spending, bill payments, or off-ramps

This swap flow is a core part of building any crypto-native experience that wants to bridge volatility and stability.

## Next

<Columns cols={2}>
  <Card title="Track orders and history" icon="list" href="/docs/trading/trade-status-and-history">
    Confirm and reconcile the swap.
  </Card>
  <Card title="Handle trading failures" icon="triangle-exclamation" href="/docs/trading/error-handling-patterns">
    Before you take this to production.
  </Card>
</Columns>
