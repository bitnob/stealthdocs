export const ProductPanel = ({ badge, badgeClass, title, description, facts, links, img }) => {
  return (
    <div className="flex flex-col rounded-2xl border border-gray-200 dark:border-white/10 overflow-hidden">
      {img && (
        <>
          <img src={`${img}-light.svg`} alt={title} className="w-full block dark:hidden" />
          <img src={`${img}-dark.svg`} alt={title} className="w-full hidden dark:block" />
        </>
      )}
      <div className="flex flex-col flex-1 p-6 gap-4">
        <div>
          <span className={`inline-flex px-3 py-1 rounded-full text-xs font-semibold ${badgeClass}`}>
            {badge}
          </span>
        </div>
        <div className="text-xl font-semibold text-gray-900 dark:text-gray-200">{title}</div>
        <p className="text-sm text-gray-600 dark:text-gray-400">{description}</p>
        <div className="flex flex-col divide-y divide-gray-100 dark:divide-white/10 text-sm">
          {facts.map(([label, value]) => (
            <div key={label} className="flex items-baseline justify-between gap-4 py-2">
              <span className="text-gray-500 dark:text-gray-400 whitespace-nowrap">{label}</span>
              <span className="font-medium text-right text-gray-900 dark:text-gray-200">{value}</span>
            </div>
          ))}
        </div>
        <div className="flex flex-wrap gap-x-5 gap-y-2 mt-auto pt-2">
          {links.map((link) => (
            <a
              key={link.href}
              href={link.href}
              className="text-sm font-semibold text-primary-dark dark:text-primary-light flex items-center gap-1.5"
            >
              {link.label} <Icon icon="arrow-right" size="12" />
            </a>
          ))}
        </div>
      </div>
    </div>
  )
}
